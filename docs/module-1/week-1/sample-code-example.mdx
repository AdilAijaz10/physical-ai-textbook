---
sidebar_label: 'Sample Code Example'
sidebar_position: 3
---

import CodeExamples from '@site/src/components/CodeExamples';
import LearningObjectives from '@site/src/components/LearningObjectives';

<LearningObjectives objectives={[
  'Understand how to implement ROS 2 publishers and subscribers',
  'Learn to use multiple programming languages in ROS 2',
  'Practice code explanation techniques'
]} />

# Sample Code Example: ROS 2 Publisher and Subscriber

## Learning Objectives

- Understand how to implement ROS 2 publishers and subscribers
- Learn to use multiple programming languages in ROS 2
- Practice code explanation techniques

## Code Examples

<CodeExamples examples={[
  {
    title: "Python Publisher",
    language: "python",
    description: "A simple ROS 2 publisher in Python",
    code: `import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalPublisher(Node):
    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = 'Hello World: %d' % self.i
        self.publisher_.publish(msg)
        self.get_logger().info('Publishing: "%s"' % msg.data)
        self.i += 1`,
    explanation: "This Python code creates a ROS 2 node that publishes a 'Hello World' message to a topic every 0.5 seconds. The node inherits from rclpy.node.Node and uses a timer to periodically execute the callback function."
  },
  {
    title: "C++ Publisher",
    language: "cpp",
    description: "A simple ROS 2 publisher in C++",
    code: `#include <chrono>
#include <functional>
#include <memory>
#include <string>
#include "rclcpp/rclcpp.hpp"
#include "std_msgs/msg/string.hpp"

using namespace std::chrono_literals;

class MinimalPublisher : public rclcpp::Node
{
public:
  MinimalPublisher()
  : Node("minimal_publisher"), count_(0)
  {
    publisher_ = this->create_publisher<std_msgs::msg::String>("topic", 10);
    timer_ = this->create_wall_timer(
      500ms, std::bind(&MinimalPublisher::timer_callback, this));
  }

private:
  void timer_callback()
  {
    auto message = std_msgs::msg::String();
    message.data = "Hello World: " + std::to_string(count_++);
    RCLCPP_INFO(this->get_logger(), "Publishing: '%s'", message.data.c_str());
    publisher_->publish(message);
  }
  rclcpp::TimerBase::SharedPtr timer_;
  rclcpp::Publisher<std_msgs::msg::String>::SharedPtr publisher_;
  size_t count_;
};`,
    explanation: "This C++ code creates a ROS 2 node that publishes a 'Hello World' message to a topic every 500 milliseconds. The node inherits from rclcpp::Node and uses a wall timer to periodically execute the callback function."
  }
]} />

## Summary

This example demonstrates how to implement the same functionality in both Python and C++ for ROS 2. Both implementations follow the same pattern of creating a publisher, setting up a timer, and publishing messages to a topic.